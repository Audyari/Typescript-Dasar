Object Type

==================

npx jest & npx tsc

npx jest ./tests/Object-Type.test.ts

Dalam pemrograman, "Object Type" mengacu pada tipe data objek, yang merupakan salah satu jenis tipe data kompleks. 
Objek adalah kumpulan dari properti dan metode yang saling terkait, di mana properti menyimpan data, dan metode mendefinisikan 
perilaku atau aksi yang dapat dilakukan pada objek tersebut.

code seder hana nya : 

describe("sayHello", () => {
  test("should log a greeting", () => {
    interface Person {
      name: string;
      age: number;
      occupation: string;
      email?: string; // properti opsional
      greet(): void;
    }

    const person: Person = {
      name: "John Doe",
      age: 30,
      occupation: "Software Engineer",
      greet: function () {
        console.log(`Hello, my name is ${this.name}.`);
      },
    };

    person.email = "john.doe@example.com";
    console.log(person.email); // Output: "john.doe@example.com"
  });
});


==============================================




